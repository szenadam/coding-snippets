#include <iostream>

using namespace std;

void quicksort(int array[]) {
    int length = sizeof(array)/sizeof(*array);

    if (length <= 1) return;

    int pivot = array[0];


}

int main() {

    return 0;
}
